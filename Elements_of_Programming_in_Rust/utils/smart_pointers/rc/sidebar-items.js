initSidebarItems({"struct":[["Rc","the actual `Rc` object"],["RcInner","`RcInner` is what an `Rc` actually stores a reference to. it is necessary in a similar but not the same way that `Ref` and `RefMut` are necessary to `RefCell` when an `Rc` is cloned we need a way to continue tracking the refcount and so we do this by storing a raw pointer to `RcInner` and when the `Rc` is cloned the raw pointer to the `RcInner` is cloned too"]]});